using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class LernColor : MonoBehaviour
{
   
    // Start is called before the first frame update
    void SetCol()
    {
        //MaterialChangerLaserPointer SetChildRendererCol(Color.white, Color.black, -0.1f);
    }

    // Update is called once per frame
    void SetColI()
    {
        
    }
}
